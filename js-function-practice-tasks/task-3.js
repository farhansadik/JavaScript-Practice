// task 3 

// Write a function called make_avg() which will take an array of integers and the size of that array and return the average of those values.

// I DID NOT FULLY UNDERSTAND WHAT IS THE PROBLEM...

function make_avg(my_Array = [10, 20, 30]) {
    for (i of my_Array) {
        let j = i + i / my_Array.length;
        console.log(j);
    }

    return i;
}

make_avg();